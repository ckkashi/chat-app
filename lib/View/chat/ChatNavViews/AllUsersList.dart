import 'package:flutter/material.dart';

class AllUsersList extends StatelessWidget {
  const AllUsersList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AllUsersList'),
    );
  }
}
