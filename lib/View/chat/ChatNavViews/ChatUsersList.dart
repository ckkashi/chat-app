import 'package:flutter/material.dart';

class ChatUsersList extends StatelessWidget {
  const ChatUsersList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ChatUsersList'),
    );
  }
}
